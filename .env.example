# Database
DATABASE_URL="postgresql://postgres:delphidev@localhost:5432/delphi?schema=public"

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET="change-me-in-production"

# App URL (used for magic link emails)
NEXTAUTH_URL="http://localhost:3000"

# Email (optional in dev - uses ethereal.email test service)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-username
# SMTP_PASS=your-password
# SMTP_FROM="GBV Indicator Study <study@example.com>"
